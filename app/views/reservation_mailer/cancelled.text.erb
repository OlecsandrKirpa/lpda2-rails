<%= t('reservation_mailer.greetings', fullname: @reservation.fullname) %>

<%= t('reservation_mailer.cancelled.body', date: l(@reservation.datetime, format: '%e %B %Y'), hour: @reservation.datetime.strftime('%k:%M'), people: @reservation.people) %>
