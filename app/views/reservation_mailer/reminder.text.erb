<%= t('reservation_mailer.greetings', fullname: @reservation.fullname) %>

<%= t('reservation_mailer.reminder.body', date: l(@reservation.datetime, format: '%e %B %Y'), hour: @reservation.datetime.strftime('%k:%M'), people: @reservation.people) %>


<%= t('reservation_mailer.changed_mind') %>

<%= t('reservation_mailer.delete_reservation') %>:

<%= @cancel_url %>